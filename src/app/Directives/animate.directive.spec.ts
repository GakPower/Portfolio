import { AnimateDirective } from './animate.directive';

describe('AnimateFromTopDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateDirective();
    expect(directive).toBeTruthy();
  });
});
